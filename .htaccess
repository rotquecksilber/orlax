# Обычный базовый .htaccess для "чистого" сайта

# Включаем обработку ошибок по умолчанию
ErrorDocument 400 default
ErrorDocument 401 default
ErrorDocument 403 default
ErrorDocument 404 default
ErrorDocument 500 default

# Отключаем переопределение URL
RewriteEngine Off

# Разрешаем доступ ко всем файлам
<FilesMatch ".*">
    Require all granted
</FilesMatch>

# Отключаем кеширование на сервере (по желанию)
<IfModule mod_headers.c>
    Header unset Cache-Control
    Header unset Pragma
</IfModule>
